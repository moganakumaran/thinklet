#!/usr/bin/env bash
# Top-level CLI wrapper — runs the SDK CLI through the project venv.
# Usage: scripts/thinklet <subcommand> [args...]
set -euo pipefail
DIR="$(cd "$(dirname "$0")/.." && pwd)"
exec "$DIR/.venv/bin/python" -m thinklet_sdk.cli "$@"
